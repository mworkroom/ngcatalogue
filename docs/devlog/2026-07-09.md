# 2026-07-09 ngcatalogue Development Log

## Work Summary

iPhone 홈 화면에서 센터 주소는 `/center/`로 분리되지만 관리자 주소가 여전히 루트 `https://price.nangok.app/`로 열리는 문제를 조사했다. 배포된 `/admin/` HTML과 관리자 manifest 자체는 정상으로 확인되었고, 관리자만 Google OAuth 복귀 흐름을 거치면서 실제 문서 경로가 다시 해시 또는 루트 기준으로 흐를 수 있는 점을 원인 후보로 좁혔다.

## Work Completed

* 관리자 Google 로그인 시작 시 OAuth `redirectTo`가 현재 해시 주소나 루트가 아니라 실제 관리자 문서 경로인 `/admin/`을 사용하도록 변경했다.
* 관리자 로그인 전 `sessionStorage`에 저장하는 복귀 경로도 `#/admin` 대신 `/admin/` 실제 경로가 되도록 바꿨다.
* OAuth callback 뒤 현재 경로가 루트로 돌아와 있으면 `/admin/`으로 다시 이동하고, 이미 `/admin/`이면 callback query/hash만 정리하도록 복귀 로직을 수정했다.
* 서비스워커 캐시 버전을 `atomy-price-v5`로 올리고, navigation fetch 실패 시 `/admin/` 요청은 관리자 HTML 캐시, `/center/` 요청은 센터 HTML 캐시를 사용하도록 fallback을 분리했다.
* README의 route 설명을 현재 custom domain 구조에 맞춰 `/`, `/center/`, `/admin/` 기준으로 갱신했다.
* Supabase Google OAuth 설정 문서에 `https://price.nangok.app/admin/`을 Allowed Redirect URL로 별도 등록해야 한다는 내용을 추가했다.
* `npm.cmd run build`로 TypeScript와 Vite 프로덕션 빌드를 확인했다.
* 빌드 산출물의 `dist/admin/index.html`, `dist/center/index.html`, `dist/sw.js`에서 관리자/센터 manifest와 새 서비스워커 캐시 버전이 반영된 것을 확인했다.

## Decisions

배포된 `/admin/` HTML은 200 응답으로 정상 제공되고 `manifest-admin.webmanifest`도 `start_url`, `scope`, `id`가 모두 `/admin/`으로 되어 있었다. 따라서 정적 파일 충돌보다는 관리자만 갖고 있는 OAuth 복귀 흐름을 우선 수정하기로 했다.

iPhone 홈 화면 추가는 해시 뒤 경로를 독립된 문서 주소로 보지 않을 수 있으므로, 관리자 로그인 후에도 주소가 `/#/admin` 형태로 남지 않게 실제 `/admin/` 경로를 유지하는 방식을 선택했다. 센터는 OAuth가 없어 같은 문제가 나타나지 않았던 것으로 판단했다.

서비스워커 fallback도 함께 바꿨다. 네트워크가 실패했을 때 모든 navigation을 루트 `index.html`로 돌리면 `/admin/` 홈 아이콘에서도 루트 문서가 내려올 수 있으므로, 모드별 HTML 엔트리를 캐시 fallback으로 분리하는 편이 안전하다.

## Current Status

로컬 코드는 관리자 OAuth 복귀 경로를 `/admin/`로 유지하도록 수정되었다. 프로덕션 빌드는 성공했고, 빌드 결과물에서도 관리자와 센터 엔트리가 각각의 manifest를 참조한다.

실제 iPhone 검증은 배포 후 다시 해야 한다. 특히 Supabase Dashboard의 Google OAuth Allowed Redirect URL에 `https://price.nangok.app/admin/`이 없으면, 코드 수정만으로는 OAuth 제공자가 callback을 루트로 되돌리는 문제를 완전히 막을 수 없다.

## Next Steps

* 변경 사항을 배포한 뒤 iPhone Safari에서 기존 관리자 홈 아이콘을 삭제하고 `/admin/`에서 새로 추가한다.
* Supabase Dashboard에서 `https://price.nangok.app/admin/`이 Allowed Redirect URL에 등록되어 있는지 확인한다.
* 관리자 Google 로그인 후 Safari 주소가 `https://price.nangok.app/admin/`로 남는지 확인한다.
* iPhone 홈 화면 관리자 아이콘을 눌렀을 때 루트 사업자 화면이 아니라 관리자 로그인/관리자 화면으로 열리는지 확인한다.

---

## Work Summary

홈 화면 아이콘 안내 문구의 번호별 줄바꿈이 화면에서 보이지 않는 문제를 확인하고, 안내 단계 문단에 줄바꿈 보존 스타일을 직접 적용했다.

## Work Completed

* `.shortcut-steps p`에 `white-space: pre-line`을 추가해 번역 문자열 안의 `\n`이 실제 줄바꿈으로 표시되도록 했다.
* 프로젝트에 Tailwind 설정과 의존성이 없어 `whitespace-pre-line` className만으로는 CSS가 생성되지 않는다는 점을 확인했다.
* `npm.cmd run build`로 TypeScript와 Vite 프로덕션 빌드를 확인했다.

## Decisions

현재 프로젝트는 별도 Tailwind 빌드가 아니라 `src/styles.css`의 일반 CSS를 사용하는 구조다. 따라서 React 요소에 유틸리티 클래스명을 추가하는 방식보다, 기존 shortcut 안내 스타일 블록에 직접 `white-space: pre-line`을 넣는 방식이 가장 작고 확실하다고 판단했다.

## Current Status

홈 화면 아이콘 안내의 Android/iPhone 단계 문구는 문자열의 `\n` 기준으로 줄바꿈되어 표시될 준비가 되었다. 이번 변경은 CSS 보강만 포함하며, 프로덕션 빌드는 성공했다.

## Next Steps

* 브라우저에서 홈 아이콘 안내 모달을 열어 번호별 줄바꿈이 보이는지 확인한다.
* 실제 브라우저 화면에서 줄 간격과 카드 높이가 자연스러운지 확인한다.

---

## Work Summary

모바일 상단 메뉴 아이콘이 Android 브라우저의 점 세 개 메뉴 안내와 혼동되는 문제를 줄이고, 관리자 화면의 상품 정렬과 검색 사용성을 보강했다.

## Work Completed

* 사업자/센터/관리자 상단 메뉴 아이콘을 점 세 개에서 톱니바퀴 모양으로 변경했다.
* 관리자 상품 목록에도 공용 상품 정렬 로직을 적용해 검색 결과에서 세트 상품이 먼저 표시되도록 했다.
* 공용 검색창에 검색어를 한 번에 지우는 X 버튼을 추가했다.
* 검색어가 X 버튼 아래로 들어가지 않도록 입력창 오른쪽 여백과 버튼 터치 영역을 조정했다.
* `npm.cmd run build`로 TypeScript와 Vite 프로덕션 빌드를 확인했다.

## Decisions

홈 화면 아이콘 안내에서 Android 사용자가 눌러야 하는 브라우저 메뉴가 점 세 개이므로, 앱 자체 메뉴에는 같은 모양을 쓰지 않기로 했다. 주 사용자가 모바일 UI에 익숙하지 않은 50-70대라는 점을 고려해 햄버거보다 설정 메뉴로 인식하기 쉬운 톱니바퀴를 선택했다.

관리자 정렬은 별도 로직을 새로 만들지 않고 사업자/센터에서 쓰는 `sortProducts`를 재사용했다. 화면마다 정렬 기준이 달라지면 운영자가 가격표와 관리자 목록을 비교할 때 혼란이 생길 수 있기 때문이다.

검색 초기화는 별도 텍스트 버튼보다 입력창 안쪽의 X 버튼이 더 즉시 이해되므로, 검색어가 있을 때만 표시되도록 했다.

## Current Status

상단 메뉴는 브라우저 점 세 개 메뉴와 다른 톱니바퀴 아이콘으로 표시된다. 관리자 상품 목록은 공용 정렬 기준을 사용하며, 검색창에는 검색어 삭제 버튼이 나타난다. 프로덕션 빌드는 성공했다.

## Next Steps

* 실제 모바일 화면에서 톱니바퀴가 브라우저 메뉴와 혼동되지 않는지 확인한다.
* 관리자 검색 결과에서 세트 상품이 기대한 순서로 먼저 보이는지 확인한다.
* 검색창 X 버튼의 터치 영역이 충분한지 모바일에서 확인한다.

---

## Work Summary

센터 가격표 출력 방향을 세로로 바꾸고, 사용자가 글씨 크기를 비교할 수 있도록 기본 출력과 큰 글씨 출력 모드를 분리했다.

## Work Completed

* 공용 검색/출력 영역에 `PDF 출력`과 `큰 글씨 출력` 버튼을 함께 표시하도록 변경했다.
* 출력 호출 로직을 `printCatalogue` 유틸리티로 분리해 큰 글씨 출력 시에만 `body.print-large` 클래스를 임시 적용하도록 했다.
* 센터 출력의 별도 A4 가로 page 설정을 제거해 기본 A4 세로 출력 흐름을 사용하도록 했다.
* 관리자 대시보드는 별도 `admin-catalogue` page를 사용하게 분리해 기존 가로 출력 방향을 유지했다.
* 큰 글씨 출력에서는 인쇄 시 본문과 센터 표의 글씨 크기, 셀 여백이 조금 더 커지도록 했다.
* `git diff --check`와 `npm.cmd run build`로 공백 오류 및 프로덕션 빌드를 확인했다.

## Decisions

센터 표는 앞으로 열이 6개까지 늘어날 수 있지만, 실제 사용자가 작은 글씨를 읽기 어려워하는 상황을 우선 고려해 세로 출력으로 전환했다. 단일 크기를 바로 확정하기보다 기본/큰 글씨 두 모드를 제공하면 실제 브라우저 인쇄 미리보기에서 어느 크기가 적절한지 비교하기 쉽다.

기존 가로 출력에서 첫 페이지가 빈 세로 페이지로 끼는 문제는 센터 화면에 named page로 landscape를 적용하는 과정과 관련될 가능성이 있어, 센터는 기본 portrait page 흐름으로 되돌렸다. 관리자는 열 수가 많아 가로 출력이 필요하므로 센터와 분리된 관리자 전용 named page를 유지했다.

## Current Status

사업자와 센터, 관리자 화면의 출력 버튼은 기본 출력과 큰 글씨 출력으로 나뉘었다. 센터 출력은 A4 세로 방향을 사용하고, 관리자 출력은 A4 가로 방향을 유지한다. 프로덕션 빌드는 성공했다.

## Next Steps

* 센터 화면에서 기본 출력과 큰 글씨 출력을 각각 미리보기로 확인해 실제 글씨 크기를 비교한다.
* 센터 출력에서 5열 및 향후 6열 구성일 때 표가 잘리지 않는지 확인한다.
* 관리자 가로 출력에서 빈 첫 페이지가 계속 생기는지 별도로 확인한다.

---

## Work Summary

사업자, 센터, 관리자 표에서 상품명 열이 좁고 숫자 열이 과하게 넓어지는 문제를 조정했다. 관리자 표에서는 일부 텍스트 헤더 정렬과 메모 열 폭도 함께 정리했다.

## Work Completed

* 사업자/센터 가격표에 `colgroup`을 추가해 상품명 열은 넓게, 숫자 열은 남은 폭을 균등 분배하도록 했다.
* 센터 표의 화면 및 인쇄 레이아웃을 `table-layout: fixed`로 바꿔 브라우저가 숫자 열을 과하게 넓히지 않도록 했다.
* 센터 인쇄 CSS에서 상품명 첫 열 폭을 기존 20%에서 46%로 변경해 상품명이 지나치게 여러 줄로 내려가지 않도록 했다.
* 관리자 표에 `colgroup`을 추가해 한국어/포르투갈어 상품명은 넓게, 숫자/세트/메모 열은 좁게 고정했다.
* 관리자 표의 상품명 br, 메모, 관리 헤더 정렬을 왼쪽으로 맞추고, 메모 열은 한 줄 말줄임 처리되도록 했다.
* `git diff --check`와 `npm.cmd run build`로 공백 오류 및 프로덕션 빌드를 확인했다.

## Decisions

상품명은 실제 사용자가 구분해야 하는 핵심 정보이고 숫자 열은 짧은 값이 대부분이므로, 동일 폭 분배보다 상품명 우선 폭 배분이 읽기 쉽다고 판단했다. 특히 센터 출력에서는 기존 인쇄용 첫 열 20% 설정이 상품명을 2-3줄로 만드는 주요 원인이어서 화면과 출력 모두 상품명 열을 넓게 유지하도록 조정했다.

관리자 메모는 긴 텍스트가 들어갈 수 있지만 목록에서 전체 내용을 모두 보여줄 필요는 낮다. 관리자 화면의 표 가독성을 위해 메모 열을 좁게 고정하고 넘치는 내용은 잘라내는 방식을 선택했다.

## Current Status

사업자, 센터, 관리자 표는 상품명 열을 우선적으로 넓게 사용한다. 관리자 표의 상품명 br, 메모, 관리 헤더 정렬은 본문과 맞춰졌고, 메모 열은 좁은 폭에서 말줄임 처리된다. 프로덕션 빌드는 성공했다.

## Next Steps

* 센터와 사업자 출력 미리보기에서 상품명 줄바꿈이 실제 데이터 기준으로 줄었는지 확인한다.
* 관리자 표에서 메모 말줄임이 운영에 불편하지 않은지 확인한다.

---

## Work Summary

PC 표 전환 시점에 센터 표 헤더 글씨가 작아지고, 첫 열 고정이 체감되지 않는 문제를 조정했다.

## Work Completed

* 센터 표 헤더에 별도로 적용되던 작은 글씨 크기 규칙을 제거해 PC 표 전환 후에도 기본 헤더 크기를 유지하도록 했다.
* 표의 `border-collapse`를 `separate`로 변경해 sticky table cell 동작이 더 안정적으로 작동하도록 했다.
* 센터/사업자 표와 관리자 표의 첫 열 sticky 셀에 배경 클리핑과 그림자를 추가해 가로 스크롤 중 고정 열이 더 분명하게 보이도록 했다.
* `git diff --check`와 `npm.cmd run build`로 공백 오류 및 프로덕션 빌드를 확인했다.

## Decisions

열이 많다는 이유로 PC 표 전환 시 글씨를 줄이면 실제 주 사용자층이 읽기 어렵다. 따라서 글씨 크기는 유지하고, 첫 열 고정과 가로 스크롤로 열이 많은 문제를 해결하는 방향을 선택했다.

sticky table cell은 브라우저별로 `border-collapse: collapse`에서 불안정할 수 있어 `separate` 모델로 바꿨다. 시각적으로 고정 열이 구분되도록 얇은 경계선과 그림자를 함께 적용했다.

## Current Status

센터/사업자/관리자 표는 PC 표 전환 후 헤더 글씨 크기가 작아지지 않는다. 첫 열은 가로 스크롤 컨테이너 안에서 sticky로 고정되도록 보강되었다. 프로덕션 빌드는 성공했다.

## Next Steps

* 브라우저 폭을 줄였다 늘리며 PC 표 전환 시 글씨 크기가 유지되는지 확인한다.
* 센터와 관리자 표에서 가로 스크롤 시 첫 열이 왼쪽에 고정되는지 확인한다.
